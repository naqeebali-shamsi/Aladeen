# Dependencies
node_modules/

# Build output
dist/

# Local run state and logs
.aladeen/
logs/
*.log

# Environment (secrets) — ignore every .env variant; allow a committed template
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local tooling artifacts (IDE assistants, MCP servers) — never commit
.cursor/
.opencode/
.playwright-mcp/

# Graphify temp + per-run cache (commit the report and graph.json, not the cache)
.graphify_python
.graphify_*.json
graphify-out/cache/
graphify-out/.graphify_chunk_*.json

# Local-only notes, specs, and changelog (keep in workspace, not in remote)
/LINKEDIN_JOURNEY_NOTES.md
/LOCAL_FIRST_AUTONOMY_SPEC.md
/LOCAL_FIRST_DELIVERY_ROADMAP.md
/PRD.md
/RESEARCH_FINDINGS.md
/BLUEPRINT_DESIGN.md
/GROUND_RULES.md
/CHANGELOG.md
